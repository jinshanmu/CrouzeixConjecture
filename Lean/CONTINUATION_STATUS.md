# Continuation status

The active source is
`../preprint/the_numerical_range_is_a_2_spectral_set_v3.tex`, 1,066 lines,
SHA-256
`f190178bc197c5f62fa8146f96c932a241379e90bf63145d542035058b92d154`.
Every line reference below is to that file.

The project uses Lean 4.28.0 and Mathlib commit
`8f9d9cff6bd728b17a24e163c9402775d9e6a365`. The committed manifest pins
the transitive dependency tree; downloaded packages and build products remain
local under `.lake/`.

## Mathematical status

The manuscript's polynomial theorem is proved for every nonempty finite
complex Euclidean matrix space:

- `crouzeixConjecture_mainTheorem : MainTheoremStatement` is the quantified
  target;
- `crouzeixConjecture A p` states
  `‖polynomialEval p A‖ ≤
  2 * maxPolynomialModulusOnNumericalRange A p`;
- `[Nonempty n]` makes the positive-dimension convention explicit;
- the norm is the induced Euclidean operator norm.

The rational spectral-set result is the separate theorem
`crouzeixRationalSpectralSetCorollary`, with pointwise form
`crouzeixRationalBound`.

## Active v3 proof route

1. The finite complex Euclidean model, numerical range, functional calculus,
   positivity, spectrum, generated algebra, compactness, spectral inclusion,
   and Toeplitz--Hausdorff convexity are proved.
2. For the positive-real completion theorem, the auxiliary matrix `B` has a
   `SimpleDiagonalization`, while the target has the form
   `T = S diag(lambda) S⁻¹` in the same basis. Only
   `‖lambda i‖ ≤ 1` is assumed; repetitions and zeros are allowed.
3. The defect condition is relative to `alg(Bᴴ)`. Pullback produces the model
   `G(I-zΛ)⁻¹ + D(z)G`. The kernel is sampled at one point for each
   `lambda i` together with zero; the arbitrary-finite-type sampling theorem
   does not require these points to be distinct.
4. The unknown diagonal correction cancels. The resulting inequality passes
   through the two Gramians, the eigenvector argument, the first nonconstant
   Gramian term, square roots,
   and polar decomposition to prove `‖T‖ ≤ 2`. In manuscript notation the
   three matrices are `P,Q,Y`; Lean calls them
   `completionP, completionR, completionX`.
5. The double-layer construction produces a positive-real completion for the
   auxiliary pair `(B, f(B))`. Its pointwise companion values lie in `alg(B)`,
   so the defect lies in `alg(Bᴴ)`; no equality with the algebra generated by
   `f(B)` is needed.
6. Polynomial functional calculus preserves the eigenbasis:
   `SimpleDiagonalization.polynomialEval_eq_innerConjugation_diagonal`.
   Thus the target entries are `lambda i = f(βᵢ)`, with no injectivity
   requirement.
7. The fixed-simple theorem
   `norm_polynomialEval_le_two_mul_of_simpleSpectrum` treats `M = 0`
   directly. For `M > 0`, it uses only `f = p / M`, invokes the
   double-layer provider and auxiliary completion theorem, and rescales.
8. The remaining limits follow the v3 order: fix a canonical convex outer
   domain, let simple-spectrum `Bₖ → A` inside that domain, and only then
   shrink the domains to `W(A)`. These prove `crouzeixConjecture_mainTheorem`.
9. The rational corollary follows separately by polynomial approximation of
   functions pole-free on the numerical range.

## Deleted superseded formalization

The earlier affine `f_eta` perturbation, finite collision-set,
interpolation-based generated-algebra equality, and `eta → 0` formalization
has been deleted from the working tree. It is not part of the current module
or declaration inventory. Git history preserves the prior implementation for
historical comparison.

## Verification procedure

Run Lean/Lake processes serially to control memory and disk use. The
authoritative checks are plain `lake build` followed by plain `lake run`;
the latter rebuilds as needed and runs `lake env lean AxiomAudit.lean`.

## Scaled `q`-numerical-range extension

The active scaled `q` source is
`../preprint/q_numerical_range_spectral_set.tex`, 660 lines, SHA-256
`5939c6f76c0d2ab0aea076ae89af7d3c709fc49ba175917560da31733f061620`.

Its exact rational spectral-set theorem is proved by
`sharpRationalScaledQNumericalRangeBound`; the polynomial specialization is
`sharpPolynomialScaledQNumericalRangeBound`.  The reusable methodological
endpoint is `rationalScaledQNumericalRangeBound_of_universal`, which transfers
an ordinary universal numerical-range constant `K` to
`max 1 (K / qKappa ‖q‖)`.

The active proof route is:

1. phase reduction and exact `q`-parameter identities;
2. an internal proof of Tsing's disk-union formula and range nesting;
3. containment of the numerical ranges of the adapted positive rank-one
   stretches;
4. scalar and matrix rational Möbius calculus plus rational spectral mapping;
5. the rank-one-stretch extraction lemma;
6. `M + epsilon` normalization and passage to the limit;
7. specialization to the proved ordinary constant `2`;
8. constant-function and Jordan-nilpotent lower bounds, combined as
   `sharpScaledQNumericalRangeConstant_isLeast_finTwo_complex`.

This is also the manuscript's complete proof route: its theorem is the exact
rational spectral-set statement, and every geometric, algebraic, extraction,
transfer, and sharpness step has a corresponding proved Lean declaration.
